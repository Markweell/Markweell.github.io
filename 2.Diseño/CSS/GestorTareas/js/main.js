{
    document.addEventListener('DOMContentLoaded', init);

    function init() {
        document.getElementById('submit').addEventListener('click',remueveEventos)
    }
    function remueveEventos(ev){
        //ev.preventDefault();
    }
}