class Empresa{

    constructor(nombre,direccion,telefono,nif){
        this.compruebaCampos(nombre,direccion,telefono,nif);
        this.nombre=nombre; 
        this.direccion = direccion; 
        this.telefono = telefono; 
        this.nif = nif;
    }
    compruebaCampos(nombre, direccion, telefono, nif) {
       
    }

}